const myName = 'ripan'

function first() {
    const age = 30
    if (age >= 30) {
        const decade = 3
        let millenial = true
    }

    function second() {
        const job = 'teacher'
        console.log(`${myName} is a ${age}-old `)
    }
    second()
}
first()
